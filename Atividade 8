import React from 'react';
import { View, Text, TouchableOpacity, SafeAreaView, StyleSheet } from 'react-native';

const theme = {
  colors: {
    primary: '#6200EE',
    text: '#333333',
    background: '#F5F5F5',
    surface: '#FFFFFF',
  },
  spacing: {
    small: 8,
    medium: 16,
    large: 24,
  }
};

export default function Ex08() {
  const cardSpacing = theme.spacing.large;

  return (
    <SafeAreaView style={styles.container}>
      <View style={[styles.card, { padding: cardSpacing }]}>
        <Text style={styles.h1}>título</Text>
        <Text style={styles.h2}>subtítulo</Text>
        <Text style={styles.body}>texto</Text>
        <Text style={styles.caption}>legenda</Text>
      </View>
      
      <TouchableOpacity style={styles.button} onPress={() => {}}>
        <Text style={styles.buttonText}>Clique Aqui</Text>
      </TouchableOpacity>
    </SafeAreaView>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: theme.colors.background,
    padding: theme.spacing.medium,
  },
  card: {
    backgroundColor: theme.colors.surface,
    borderRadius: 8,
    marginBottom: theme.spacing.medium,
    shadowColor: '#000',
    shadowOffset: { width: 0, height: 2 },
    shadowOpacity: 0.1,
    shadowRadius: 4,
    elevation: 3,
  },
  h1: {
    fontSize: 24,
    fontWeight: 'bold',
    color: theme.colors.text,
    marginBottom: theme.spacing.small,
  },
  h2: {
    fontSize: 20,
    fontWeight: '600',
    color: theme.colors.text,
  },
  body: {
    fontSize: 16,
    color: theme.colors.text,
    marginVertical: 4,
  },
  caption: {
    fontSize: 12,
    color: '#888888',
    marginTop: 4,
  },
  button: {
    backgroundColor: theme.colors.primary,
    padding: theme.spacing.medium,
    borderRadius: 8,
    alignItems: 'center',
  },
  buttonText: {
    color: '#FFFFFF',
    fontWeight: 'bold',
    fontSize: 16,
  },
});
